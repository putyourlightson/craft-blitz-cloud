{% set testErrors = purger.getErrors('test') ?? '' %}
{% if testErrors is not empty %}
    <ul class="errors">
        {% for error in testErrors %}
            <li>{{ error }}</li>
        {% endfor %}
    </ul>
{% endif %}
